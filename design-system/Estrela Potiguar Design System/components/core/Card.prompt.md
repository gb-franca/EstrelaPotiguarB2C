Elevated surface container. Use `interactive` for clickable cards (hover-lift), `glow` to swap the lift shadow for the warm star halo.

```jsx
<Card padding="md" interactive glow>…</Card>
```

Props: `padding` (`none|sm|md|lg`), `interactive`, `glow`.
