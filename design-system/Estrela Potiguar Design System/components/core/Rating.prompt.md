Five-star rating in the brand yellow. Read-only by default; pass `onChange` to make it an input.

```jsx
<Rating value={4.8} count={1240} showValue />
<Rating value={pick} onChange={setPick} size={28} />
```

Props: `value`, `max` (5), `size`, `count`, `showValue`, `onChange`.
