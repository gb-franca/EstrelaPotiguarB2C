Selectable category chip — the map's filter row (Praias, Fortes, Dunas, Gastronomia…).

```jsx
<Tag icon={<WavesIcon/>} selected onClick={toggle}>Praias</Tag>
<Tag icon={<LandmarkIcon/>} color="var(--aurora)">Fortes</Tag>
```

Props: `icon`, `selected`, `color` (per-category accent), `onClick`.
