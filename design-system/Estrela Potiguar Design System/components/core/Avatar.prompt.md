Round avatar; falls back to initials (or a star) when no `src`.

```jsx
<Avatar name="Ana Lima" src={url} ring />
<Avatar name="João" size={32} />
```

Props: `src`, `name`, `size`, `ring` (yellow glow ring).
