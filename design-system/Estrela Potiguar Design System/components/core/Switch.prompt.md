Toggle switch (settings, filters). Track lights star-yellow when on.

```jsx
<Switch checked={open} onChange={setOpen} label="Só abertos agora" />
```

Props: `checked`, `onChange(next)`, `size` (`sm|md`), `label`, `disabled`.
